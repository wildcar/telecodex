# FS.md

The functional specification has moved to `AGENTS-TECHSPEC.md`.

Agents should update `AGENTS-TECHSPEC.md` before code or behavior changes when the requested task changes the functional contract.
